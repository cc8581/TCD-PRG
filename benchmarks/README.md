# Benchmark fixtures

This directory contains small, versioned inputs required to reproduce resource
and throughput measurements. Generated profiler outputs belong under
`outputs/profiles/` and are ignored by Git.

- `dataset_group_snapshot.json`: fixed group-count snapshot consumed by
  `tools/profile_training.py`.
